# Lumi Studio – Full Working App (Vercel + Stripe)

This is a REAL web app (frontend + backend).

Deploy:
1. Push repo to GitHub
2. Import into Vercel
3. Add ENV vars:
   STRIPE_SECRET_KEY
   STRIPE_PRICE_ID
   STRIPE_WEBHOOK_SECRET
   APP_URL

0 fixed cost.